---
id: 5.2
status: closed
effort: medium
type: refactoring
priority: 2
deps: [5.1]
links: []
created: 2026-01-12T16:27:00Z
parent: 5
assignee: Will Kelly
---
# Extract useEditorLinter hook

Extract linting logic from USFMPlugin.tsx into src/app/domain/editor/hooks/useEditorLinter.ts.

Logic to extract:
- debouncedLint function (USFMPlugin.tsx lines 55-62)
- editor.registerUpdateListener for linting (USFMPlugin.tsx lines 196-204)
- lintDebounceMs constant (USFMPlugin.tsx line 51)

The hook must:
1. Accept editor: LexicalEditor as argument
2. Use useWorkspaceContext internally for lint and actions.getFlatFileTokens
3. Handle mode check: if (mode !== EditorModes.WYSIWYG) return
4. Call lintAll() with editorState, editor, actions.getFlatFileTokens
5. Merge errors via lint.mergeInNewErrorsFromChapter(errMessages)
6. Export function useEditorLinter(editor: LexicalEditor)

Reference files:
- src/app/domain/editor/plugins/USFMPlugin.tsx
- src/app/domain/editor/listeners/lintChecks.ts

## Design

Use @mantine/hooks useDebouncedCallback for the lint debounce (300ms).
Register update listener inside useEffect.
Return cleanup function that calls the unregister function.

## Acceptance Criteria

useEditorLinter hook created and exported. Hook uses useWorkspaceContext, accepts editor parameter, handles WYSIWYG mode check, debounces linting, and merges errors into lint state.



## Notes

**2026-01-12T16:43:04Z**

Verified by QA: All acceptance criteria met. Architecture compliant. Biome and tests pass.
