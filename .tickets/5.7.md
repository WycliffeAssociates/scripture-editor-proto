---
id: 5.7
status: closed
effort: low
type: testing
priority: 1
deps: [5.6]
links: []
created: 2026-01-12T16:28:00Z
parent: 5
assignee: Will Kelly
---
# Run unit tests to verify refactoring

Execute unit test suite to ensure no regressions from the USFMPlugin refactoring.

Steps:
1. Run pnpm test:unit
2. Check for test failures, especially in:
   - src/app/domain/editor/**/*test.ts
   - src/app/domain/editor/listeners/**/*test.ts
   - src/app/domain/editor/nodes/**/*test.ts
3. If tests fail, investigate the failure:
   - Check if any tests directly reference USFMPlugin internals
   - Check if any tests mock USFMPlugin behavior
   - Verify hook cleanup is working correctly
4. Fix any test failures caused by the refactoring
5. Ensure all tests pass

If tests fail, the issue is likely in one of the extracted hooks or the USFMPlugin update. Common issues:
- Hooks not registering listeners correctly
- Cleanup functions not returning unregister functions
- Mode checks or tag filtering not working as expected

Dependencies: Must wait for USFMPlugin.tsx update (ticket 5.6).

## Design

No new tests are required - this is verification that existing tests still pass after refactoring.
If tests fail and need to be updated, the updates should focus on testing the new hooks directly rather than the monolithic plugin.

## Acceptance Criteria

All unit tests pass. No regressions in editor functionality tests.

