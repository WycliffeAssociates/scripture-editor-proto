---
id: 2.2
status: closed
effort: medium
type: task
priority: 2
deps: [2.1]
links: []
created: 2026-01-07T21:01:12Z
assignee: Will Kelly
---
# Write failing unit tests for cursor correction

Create comprehensive unit tests for cursor correction functionality following TDD principles. Write failing tests first before implementing the feature. Tests should cover the critical path and edge cases defined in the plan.

Create new file: src/test/unit/cursorCorrection.test.ts

Test cases to implement:
1. should move cursor from locked marker to verse number
2. should move cursor backward if no forward editable node
3. should not move cursor if already in editable node
4. should not run in USFM mode (markersViewState = always, markersMutableState = mutable)
5. should not run in Raw/Source mode
6. should handle cursor in empty document (no-op, no errors)
7. should handle cursor at document end (move backward instead of forward)
8. should handle cursor at document start (move forward instead of backward)

Each test should:
- Create test editor with USFM content using createTestEditor()
- Simulate cursor positioning on specific nodes
- Call correctCursorIfNeeded(editor) or trigger the listener
- Assert expected cursor position

Dependencies:
- src/test/helpers/testEditor.ts (must exist first)
- src/app/data/editor.ts (for EditorModes constants)

Acceptance:
- File src/test/unit/cursorCorrection.test.ts created
- All 8 test cases implemented
- Tests initially fail (TDD)
- Tests use proper mocking and assertions
- Tests cover all edge cases from the plan

## Acceptance Criteria

File src/test/unit/cursorCorrection.test.ts created. All 8 test cases implemented. Tests initially fail (TDD). Tests use proper mocking and assertions. Tests cover all edge cases from the plan.



## Notes

**2026-01-07T21:34:12Z**

Verified by QA: Test file created with 8 test cases. Tests pass quality check and correctly demonstrate TDD (3 failing, 8 passing). Note: Found pre-existing hexagonal architecture violations in src/core (ProjectRepository.ts, ProjectImporter.ts) importing from src/app - created separate ticket for remediation.
