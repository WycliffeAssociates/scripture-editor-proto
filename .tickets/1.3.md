---
id: 1.3
status: closed
effort: medium
type: task
priority: 2
deps: [1.2]
links: []
created: 2026-01-07T21:02:04Z
assignee: Will Kelly
---
# Make cursor correction tests pass

Run the unit tests written earlier and fix any failing tests. Debug issues, adjust implementation as needed, and ensure all tests pass. This is the TDD "red-green-refactor" step where we go from failing tests to passing tests.

Steps:
1. Run pnpm test:unit to execute cursor correction tests
2. Identify failing tests and specific issues
3. Debug and fix implementation in USFMPlugin.tsx
4. Re-run tests iteratively until all pass
5. Run pnpm biome to ensure code quality
6. Verify no regressions in existing tests

Common issues to check:
- Cursor selection API usage (selectStart() vs selectEnd())
- Mode detection (WYSIWYG vs USFM vs Source)
- Editor state read() vs update() timing
- Node traversal edge cases (document boundaries)
- Mocking in tests

Files to potentially adjust:
- src/app/domain/editor/plugins/USFMPlugin.tsx (implementation fixes)
- src/test/unit/cursorCorrection.test.ts (test refinements if needed)

Acceptance:
- All 8 cursor correction unit tests pass
- No existing test regressions
- pnpm biome passes without errors
- Code follows existing patterns in maintainDocumentStructure.ts
- Edge cases properly handled (document boundaries, empty docs, nested structures)

## Acceptance Criteria

All 8 cursor correction unit tests pass. No existing test regressions. pnpm biome passes without errors. Code follows existing patterns in maintainDocumentStructure.ts. Edge cases properly handled (document boundaries, empty docs, nested structures).



## Notes

**2026-01-08T16:02:52Z**

Verified by QA: All 11 cursor correction tests passed. Biome check passes on files changed (USFMPlugin.tsx, cursorCorrection.test.ts). No architecture violations. Code follows existing patterns in maintainDocumentStructure.ts.
