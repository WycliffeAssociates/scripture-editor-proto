---
id: 1.3
status: closed
effort: medium
type: task
priority: 2
deps: [1]
links: []
created: 2026-01-12T16:17:52Z
parent: 1
assignee: Will Kelly
---
# Refactor ReferencePicker component to use TEST_ID_GENERATORS

Update src/app/ui/components/blocks/ReferencePicker.tsx to use the TEST_ID_GENERATORS.bookChapterBtn generator for book-control- IDs and any other dynamic IDs found in the component.

## Design

Replace inline template literals in ReferencePicker.tsx with the appropriate generators from TEST_ID_GENERATORS. Ensure all dynamic IDs are covered.

## Acceptance Criteria

ReferencePicker.tsx uses TEST_ID_GENERATORS for generating book-control- IDs and other dynamic IDs.



## Notes

**2026-01-12T16:51:11Z**

Implementation verified by QA:
- Added TEST_ID_GENERATORS import
- Replaced inline template literal `book-control-title-${book}` with TEST_ID_GENERATORS.bookTitle(book)
- Replaced inline template literal `book-${book}-chapters` with TEST_ID_GENERATORS.bookChapterPanel(book)
- Replaced inline template literal `book-control-${book}-${chap}` with TEST_ID_GENERATORS.bookChapterBtn(book, chap)
- Removed unnecessary template literals for static IDs
- All dynamic IDs now use generators
- Biome linting passed
- Tests passed
