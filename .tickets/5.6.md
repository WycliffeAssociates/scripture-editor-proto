---
id: 5.6
status: closed
effort: medium
type: refactoring
priority: 1
deps: [5.2, 5.3, 5.4, 5.5]
links: []
created: 2026-01-12T16:27:45Z
parent: 5
assignee: Will Kelly
---
# Refactor USFMPlugin.tsx to use extracted hooks

Update src/app/domain/editor/plugins/USFMPlugin.tsx to use the newly extracted hooks.

Changes required:
1. Remove all extracted logic from USFMPlugin.tsx:
   - Remove markersInPreview ref (line 50)
   - Remove all constants (lintDebounceMs, sixtyFPS, structuralUpdateDebounceMs)
   - Remove debouncedLint, debouncedStructuralUpdates, throttledEditorChangeListener functions
   - Remove entire useEffect body (lines 94-269)
   - Remove dependencies array from useEffect (lines 270-280)
2. Add imports for all four hooks:
   - import { useEditorLinter } from '@/app/domain/editor/hooks/useEditorLinter'
   - import { useEditorStructure } from '@/app/domain/editor/hooks/useEditorStructure'
   - import { useEditorInput } from '@/app/domain/editor/hooks/useEditorInput'
   - import { useEditorView } from '@/app/domain/editor/hooks/useEditorView'
3. Keep existing context providers:
   - useLexicalComposerContext
   - useWorkspaceContext (for destructuring)
4. In component body (after context hooks), call each hook:
   - useEditorLinter(editor)
   - useEditorStructure(editor)
   - useEditorInput(editor)
   - useEditorView(editor)
5. Ensure component still returns null
6. Ensure no remaining duplicates or orphaned code
7. Run pnpm biome to ensure proper formatting

The component should now be a thin composition layer that orchestrates the four hooks. Each hook handles its own useEffect and cleanup.

Reference file: src/app/domain/editor/plugins/USFMPlugin.tsx

Dependencies: Must wait for all four hook extraction tickets to complete (tickets 5.2, 5.3, 5.4, 5.5).

## Design

The refactored component should be approximately 15-20 lines total.
All cleanup logic should be handled internally by the hooks.
The component should have no useEffect of its own.

## Notes

**2026-01-12T20:15:00Z**

Verified by QA: USFMPlugin.tsx successfully refactored into thin composition layer. All acceptance criteria met:
- ✅ All extracted logic removed (refs, constants, functions, useEffect)
- ✅ All four hooks imported and called with editor parameter
- ✅ Component returns null
- ✅ No orphaned code or duplicates
- ✅ Component compiles without errors
- ✅ Biome formatting passes (exit code 0, only warnings)
- ✅ Unit tests pass (140/140 passed, 1 worker error unrelated to refactoring)
- ✅ Architecture verified: No src/core imports from src/app

