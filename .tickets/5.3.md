---
id: 5.3
status: closed
effort: medium
type: refactoring
priority: 2
deps: [5.1]
links: []
created: 2026-01-12T16:27:10Z
parent: 5
assignee: Will Kelly
---
# Extract useEditorStructure hook

Extract document structure maintenance logic from USFMPlugin.tsx into src/app/domain/editor/hooks/useEditorStructure.ts.

Logic to extract:
- debouncedStructuralUpdates function (USFMPlugin.tsx lines 64-73)
- throttledEditorChangeListener function (USFMPlugin.tsx lines 75-92)
- editor.registerUpdateListener calls for maintainMetadata (USFMPlugin.tsx lines 100-124)
- editor.registerUpdateListener calls for debouncedMaintainMetadata (USFMPlugin.tsx lines 125-149)
- Constants: sixtyFPS (line 52), structuralUpdateDebounceMs (line 53)

The hook must:
1. Accept editor: LexicalEditor as argument
2. Use useWorkspaceContext internally for project.pickedFile.bookCode
3. Handle mode check: if (mode === EditorModes.SOURCE) return (early return)
4. Handle tags filtering:
   - if (tags.has(EDITOR_TAGS_USED.programmaticDoRunChanges)) skip when appropriate
   - if (tags.has(EDITOR_TAGS_USED.programaticIgnore)) skip
   - if (dirtyElements.size === 0 && dirtyLeaves.size === 0 && !tags.has(programmaticDoRunChanges)) skip
5. if (prevEditorState.isEmpty()) skip
6. Call maintainDocumentStructureDebounced(editorState, editor) via debouncedStructuralUpdates
7. Call maintainDocumentStructure(editorState, editor) via throttledEditorChangeListener
8. Call maintainDocumentMetaData(editorState, editor, bookCode) via throttledEditorChangeListener
9. Export function useEditorStructure(editor: LexicalEditor)

Reference files:
- src/app/domain/editor/plugins/USFMPlugin.tsx
- src/app/domain/editor/listeners/maintainDocumentStructure.ts
- src/app/domain/editor/listeners/maintainMetadata.ts
- src/app/data/editor.ts (for EDITOR_TAGS_USED, EditorModes)

## Design

Use @mantine/hooks useDebouncedCallback (1000ms) and useThrottledCallback (16ms).
Must preserve the exact timing behavior of throttled (60fps) and debounced (1000ms) updates.
Register both update listeners inside useEffect.
Return cleanup function that calls both unregister functions.

## Acceptance Criteria

useEditorStructure hook created and exported. Hook uses useWorkspaceContext for bookCode, handles SOURCE mode early return, implements all tag/selection checks, debounces structural updates (1000ms), throttles metadata updates (60fps), and calls maintainDocumentStructure/maintainDocumentMetaData correctly.



## Notes

**2026-01-12T16:53:32Z**

Verified by QA: useEditorStructure hook successfully extracted from USFMPlugin.tsx. All acceptance criteria met:
- Hook created and exported from src/app/domain/editor/hooks/useEditorStructure.ts
- Uses useWorkspaceContext for bookCode
- Handles SOURCE mode early return
- Implements all tag/selection checks (wasOnlySelChange, programmaticDoRunChanges, prevEditorState.isEmpty, programaticIgnore)
- Debounces structural updates (1000ms) using useDebouncedCallback
- Throttles metadata updates (60fps/16ms) using useThrottledCallback
- Correctly calls maintainDocumentStructure and maintainDocumentMetaData
- No biome errors in the hook file
- All unit tests pass (95 tests)
- No architecture violations (src/core does not import src/app)
- Code follows hexagonal architecture principles
