---
id: 3.1
status: closed
effort: medium
type: task
priority: 2
deps: []
links: []
created: 2026-01-12T16:18:10Z
parent: 3
assignee: Will Kelly
---
# Analyze lexicalToUsfm.ts for closure variables

Read through the buildSidContentMapForChapter function in src/app/domain/editor/serialization/lexicalToUsfm.ts and identify all closure variables used in the loop. Document these variables for the refactoring process.

## Design

Focus on the loop in buildSidContentMapForChapter and identify variables like activeVerseKey, previousBlockKey, duplicateSidCounters, footnoteCounters, and blockCounter.

## Acceptance Criteria

A list of closure variables and their roles is documented in the ticket notes.



## Notes

**2026-01-12T16:29:25Z**

## Closure Variables Analysis

The following closure variables are used within the `for (let i = 0; i < chapterNodeList.length; i++)` loop in `buildSidContentMapForChapter`:

### 1. **map** (SidContentMap)
- **Type:** `Record<string, SidContent>`
- **Purpose:** The accumulator that stores all discovered blocks (verses, footnotes, etc.)
- **Usage:**
  - Retrieves parent verse info: `map[activeVerseKey].semanticSid` (line 310)
  - Stores new blocks: `map[footnoteKey] = {...}` (line 317)
  - Checks for duplicates: `if (map[uniqueKey])` (line 344)
  - Updates block content: `map[activeVerseKey] = {...}` (line 374)
  - Appends nodes to existing blocks: `block.nodes.push(node)` (line 392)
  - Retrieves previous block for anchor: `const prevBlock = map[previousBlockKey]` (line 355)

### 2. **activeVerseKey** (string | null)
- **Type:** `string | null`
- **Purpose:** Tracks the currently active verse block's unique key
- **Usage:**
  - Determines footnote parent: `if (activeVerseKey)` (line 309)
  - Checks if we need to start a new verse block: `if (!activeVerseKey || map[activeVerseKey].semanticSid !== semanticSid)` (line 339-342)
  - Updated when new verse encountered: `activeVerseKey = uniqueKey` (line 350)
  - Used to append follower nodes: `if (activeVerseKey)` (line 405)

### 3. **previousBlockKey** (string | null)
- **Type:** `string | null`
- **Purpose:** Tracks the key of the previous block for linking/anchoring
- **Usage:**
  - Checks for out-of-order detection: `if (previousBlockKey)` (line 354)
  - Retrieves previous block: `const prevBlock = map[previousBlockKey]` (line 355)
  - Set as previousSid in new blocks: `previousSid: previousBlockKey` (lines 321, 378)
  - Updated after each new block: `previousBlockKey = footnoteKey` (line 331), `previousBlockKey = activeVerseKey` (line 388)

### 4. **duplicateSidCounters** (Map<string, number>)
- **Type:** `Map<string, number>`
- **Purpose:** Counts occurrences of duplicate semantic SIDs to create unique keys
- **Usage:**
  - Gets current count: `(duplicateSidCounters.get(semanticSid) || 0)` (line 345)
  - Updates counter: `duplicateSidCounters.set(semanticSid, count)` (line 347)
  - Creates unique key: `uniqueKey = ${semanticSid}_dup_${count}` (line 348)

### 5. **footnoteCounters** (Map<string, number>)
- **Type:** `Map<string, number>`
- **Purpose:** Counts footnotes per parent SID for creating unique footnote keys
- **Usage:**
  - Gets current count: `(footnoteCounters.get(parentSid) || 0)` (line 312)
  - Updates counter: `footnoteCounters.set(parentSid, footnoteCount)` (line 313)
  - Creates footnote key: `const footnoteKey = ${parentSid}_${node.marker}_${footnoteCount}` (line 314)

### 6. **blockCounter** (number)
- **Type:** `number`
- **Purpose:** Zero-based index tracking the order blocks were discovered
- **Usage:**
  - Set on footnote: `foundOrder: blockCounter` (line 328)
  - Incremented after footnote: `blockCounter++` (line 332)
  - Set on verse: `foundOrder: blockCounter` (line 384)
  - Incremented after verse: `blockCounter++` (line 387)

### Loop Index Variable
- **i** (number): Loop index - used to access `chapterNodeList[i]` and set `startIndexInParent`, `wrapperStartIndex`

---

### Variable Interaction Summary:
- `map` is the central accumulator storing all blocks
- `activeVerseKey` points to the current verse being built
- `previousBlockKey` links blocks sequentially
- `duplicateSidCounters` and `footnoteCounters` handle unique key generation
- `blockCounter` provides discovery ordering
- These closures maintain state across iterations without recursion

**2026-01-12T16:36:20Z**

Verified by QA: Comprehensive analysis of 7 closure variables (map, activeVerseKey, previousBlockKey, duplicateSidCounters, footnoteCounters, blockCounter, and loop index i) with accurate usage patterns documented. Documentation is accurate and covers all closure variables used in the buildSidContentMapForChapter loop.
