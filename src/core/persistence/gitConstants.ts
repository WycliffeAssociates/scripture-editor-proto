/**
 * Shared git defaults used when the app scaffolds or updates editable scripture
 * repositories.
 */
export const GIT_DEFAULT_BRANCH = "master" as const;
export const GIT_REMOTE_DEFAULT_NAME = "origin" as const;

export const GIT_COMMIT_AUTHOR = {
    name: "Dovetail",
    email: "dovetail@wycliffeassociates.org",
};
