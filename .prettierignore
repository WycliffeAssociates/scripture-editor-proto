# oxfmt reads this file by default (alongside .gitignore). Keep it to things
# oxfmt should NOT touch — generated output and tool-owned config — so a
# repo-wide `oxfmt .` stays a no-op on them instead of churning indentation.

# Generated by the TanStack Router plugin; owned by the generator's own style.
src/app/generated/routeTree.gen.ts

# Tool-managed config committed at 4-space. oxfmt's 2-space would reindent the
# whole file on every run (and CI doesn't run format, so it never gets caught);
# syncVersions / Tauri own these, so leave them be.
package.json
src/tauri/rust/tauri.conf.json
